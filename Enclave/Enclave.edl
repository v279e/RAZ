enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* define ECALLs here. */
        public void get_elem([out, size=len] char* buffer, int len, int index);

    };

    untrusted {
        /* define OCALLs here. */

    };
};
